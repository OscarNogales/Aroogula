# FinBERT Training Data

`FINBERT_V3_TRAINING_DATA.xlsx` is the dataset used by the public FinBERT training notebooks.

Older intermediate workbooks are intentionally not included in this cleaned notebook bundle because the V3 workbook supersedes them for the current training workflow.
